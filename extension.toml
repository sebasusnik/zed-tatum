id = "tatum"
name = "Tatum"
version = "0.1.0"
schema_version = 1
description = "Syntax highlighting for Tatum, the .synth music language"
authors = ["Sebastian Susnik <sebastian.susnik@winclap.com>"]
repository = "https://github.com/sebasusnik/zed-tatum"
languages = ["languages/tatum"]

# Zed clones the grammar at this revision and compiles it, caching the build
# per revision -- so bump `rev` whenever the grammar changes, or the old build
# stays. sync.sh does that for you.
[grammars.tatum]
repository = "https://github.com/sebasusnik/tree-sitter-tatum"
rev = "2dfddb9b31205ee121e0cf7f9994233369b21049"
